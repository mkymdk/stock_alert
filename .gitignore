# clasp project binding and auth — generated locally, not shared
.clasp.json
.clasprc.json

# OAuth credentials (never commit)
creds.json

# Node modules
node_modules/

# OS artifacts
.DS_Store
Thumbs.db
